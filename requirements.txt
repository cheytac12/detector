# Sign Language Detection — requirements
# Compatible set: Python 3.9 – 3.12
#
# Key constraints:
#   • mediapipe 0.10.x requires numpy < 2.0
#   • mediapipe 0.10.x requires protobuf >= 4.25.3 and < 5.0
#   • tensorflow 2.15.x requires Python <= 3.11
#   • tensorflow 2.16+ supports Python 3.12 but uses keras 3
#   • We use tensorflow 2.15 + keras 2 for full notebook compatibility
#   • If you're on Python 3.12, the launcher will auto-switch to tf 2.16
#
# Install: pip install -r requirements.txt

tensorflow==2.15.1
mediapipe==0.10.21
opencv-python==4.9.0.80
opencv-contrib-python==4.9.0.80
numpy==1.26.4
Pillow==10.4.0
scikit-learn==1.4.2
scipy==1.13.1
protobuf>=4.25.3,<5.0.0
pyttsx3==2.90
